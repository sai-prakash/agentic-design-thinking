# Primary LLM — Claude Sonnet (reasoning stages)
ANTHROPIC_API_KEY=sk-ant-...

# Fallback / Generation LLM — Gemini 2.5 Flash
# Used as fallback when ANTHROPIC_API_KEY is not set
# Also used directly for Ideate + Prototype stages (Day 2)
GOOGLE_AI_API_KEY=your-gemini-key-here
